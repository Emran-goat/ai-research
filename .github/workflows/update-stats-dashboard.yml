name: Update Stats Dashboard
on:
  push:
    branches: [master]
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:
permissions:
  contents: write
  issues: read
  pull-requests: read
jobs:
  stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Collect Stats
        run: echo "stats collected"
